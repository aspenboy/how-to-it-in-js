var contactPage = require('./pages/ContactPage');

describe('Contact Page', function () {
    
    beforeEach(function () {
        contactPage.load();
    });

    it("should have 'Get in touch' and 'Address' sections on Contact Us page", function () {
        fail();
    });

    it('should be possible to fill in and submit the form', function () {
        fail();
    });
});